{
  "name": "@wzlin/multipart",
  "description": "Pure JS multipart/form-data parser",
  "version": "0.0.1",
  "main": "dist/main.js",
  "files": [
    "dist/**"
  ],
  "repository": "https://github.com/wilsonzlin/multipart",
  "author": "Wilson Lin <npm@wilsonl.in>",
  "license": "ISC",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "format": "prettier --ignore-path .gitignore --write '**/*.{css,html,js,json,jsx,mjs,mjsx,sass,scss,ts,tsx}'",
    "prepublishOnly": "npm run build && npm run test",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.6",
    "jest": "^27.4.5",
    "prettier": "2.5.1",
    "ts-jest": "^27.1.2",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@xtjs/lib": "^0.39.5"
  }
}
